version: '3.7'

services: 
    web_app:
        build: ./web_app
        command: python app.py
        restart: unless-stopped
        volumes: 
            - ./web_app:/usr/src/app
        ports:
            - 5000:5001
        env_file: 
            - ./.env.dev